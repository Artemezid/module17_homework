import {multiply} from'../sum';

describe('test 2', () =>{
    it('adds 1 * 0 to equal 1', () => {
        expect(multiply(1, 0)).toBe(0);
      }),
      it('adds 1 * 0 to equal 1', () => {
        expect(multiply(1, 0)).toBe(0);
      });
})

